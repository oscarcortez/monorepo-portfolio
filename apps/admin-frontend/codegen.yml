schema: ${NEXT_PUBLIC_GRAPHQL_URL}
documents:
  - 'app/graphql/**/*.gql'
  - 'app/graphql/**/*.graphql'
generates:
  app/graphql/generated/graphql-types.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      # - 'typescript-react-apollo'
    config:
      avoidOptionals: true